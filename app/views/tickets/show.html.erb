<div class="row justify-content-center mb-3">
  <div class="col-md-8">
    <%= render object: @ticket, partial: "card", as: :ticket %>
  </div>
</div>

  <!-- Show Page Customization starts -->
<div class="row justify-content-center mb-3">
  <div class="col-md-8">
    <%= render partial: "tickets/ticket_bought_tickets" %>
  </div>
</div>

<div class="row justify-content-center mb-3">
  <div class="col-md-8">
    <%= render partial: "tickets/ticket_auction_bids" %>
  </div>
</div>
  <!-- Show Page Customization end -->
